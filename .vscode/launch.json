{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug GB",
            "program": "${workspaceRoot}/build/GameBoyDevEmulator",
            "type": "lldb-dap",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "args": [
                "${workspaceRoot}/games/cpu_instrs.gb"
            ],
            "env": [],
            "preLaunchTask": "build"
        }
    ]
}